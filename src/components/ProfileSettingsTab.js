import React from "react";
import { Card } from "antd";

const ProfileSettingsTab = () => (
  <Card title="Profile & Settings" bordered={false}>
    <p>Update your mentor profile and settings.</p>
  </Card>
);

export default ProfileSettingsTab;
